var ftool = require('../file-tools');

var fileGroup = function() {
    var target = [];
    return {
        get: function(){
          return target; 
        },
        strategy: function(file, group, path) {
            if (group.indexOf(file) !== -1) 
                target.push({
                    status: true,
                    path: path + '/' + file
                });
        }
    }
};

ftool.search('/usr', ['cat', 'grep', 'bin'],
    function(file, group, path) {



    });
